
ubuntu@서버IPAddress
ssh -i C:\Users\계정명\.ssh\LightsailDefaultKey-ap-northeast-2.pem ubuntu@서버IPAddress
(ssh: secure shell / -i: identity)

sudo lastb // 로그인 실패 기록
sudo lastb | wc -l


Windows: PC용 O/S. 보통 혼자 사용. 나 = 사용자 = 관리자
Linux/Unix: Server O/S. 나 = 사용자 != 관리자 (superuser = su)
sudo: su (관리자) 권한으로 실행 (do)
superuser 계정 = root
내가 관리자이면 root 계정을 사용하면 되겠네 → X
관리자 권한이 필요하지 않은 평소에는 관리자도 사용자 계정을 사용하는 게 안전하다.

Ubuntu Linux는 리눅스를 누구나 사용할 수 있도록 쉽게 만들자
그런데 root 계정을 쓰면 위험한 일이 발생할 수 있으니 root 계정을 없어버리자 (리눅스 최초)



whoami // 내가 누구인지 내 계정명 확인
id  // 내 계정의 권한 확인

whoami (현재) 결과: ubuntu
sudo whoami 결과: root (sudo를 할 때는 내가 내가 아니고 잠시 root 권한을 빌려와서 superuser가 되는 셈)

cat /etc/passwd  사용자 계정
(90년대 등 오래 전에는 passwd 파일에 사용자의 계정 + 비밀번호가 보였다.
※ 문제: 서버에 접속한 누구나 타인 계정의 비밀번호를 볼 수 있었다.
해결책으로 나온 방법: /etc/shadow 파일에 비밀번호를 분리)
cat /etc/shadow  못 본다.
sudo cat /etc/shadow  관리자 권한으로 실행하므로 보인다. (관리자는 어떤 작업이든 할 수 있다.)


** 파일 권한 **
사용자 접근 권한 구분
User (소유자) / Group (그룹) / Other (그 외 나머지)
ls -al 했을 때 나오는 결과 목록 중 3, 4번째가 각각 소유자, 그룹 (나머지는 표시할 필요가 없어서생략)

d: directory
l: link

d rwx rwx r-x 2 ubuntu ubuntu 4096 jAN 20 15:37 SESAC
소유자(ubuntu): 이 디렉토리에 read, write, execute(실행 권한)
							  access (폴더인 경우 접근 권한) 가능
나머지(other): 이 디렉토리에 read, execute/acess 가능 (쓰기(write) 권한은 없음)


cf.  which whoami // whoami 명령어가 어디 있는지 확인
결과: /usr/bin/whoami

ls -al /usr/bin/whoami 결과: -rwxr-xr-x 1 root root 35336 Jun 23  2025 /usr/bin/whoami
root: 모든 작업 가능
ubuntu (other 그룹): read, execute만 가능

ls -al /etc/passwd
-rw-r--r-- 1 root root 1850 Jan 20 11:19 /etc/passwd
root: read, write만 가능
group, other: read만 가능

-rw-r----- 1 root shadow 899 Jan 20 11:19 /etc/shadow
root: read, write만 가능
shadow 그룹: read만 가능 (※ 특이하게 그룹이 소유자와 다른 경우)


touch:
원래는 개발자 도구
c 파일을 compile해야 한다.
hello.c, admin.c, computer.c, student.c 등 파일을 컴파일해서 ERP를 만들려고 할 때 일일이 다 하기는 힘들다.
hello.o, admin.o, 후략 (컴파일된 파일)
최근에 컴파일한 적이 있는지 기억. 시간을 비교해서 전에 컴파일했는지, 이후에 변경된건지 판단
이때 변경하지 않은 파일도 변경한 것처럼 터치해서 접근 시간을 변경해서 강제 컴파일하는 도구
원래 목적은 이랬는데, 이 명령어로 파일 생성도 가능하고, 요새는 그 용도로 더 쓰이는 것 같다.

새 파일이 생성될 때 부여되는 기본 권한: 664
-rw-rw-r-- // 664 (사실은 0664)
umask 002
666 (파일 기본값 (bit가 1로 꽉 찬 값) - umask = 664
777 (폴더 기본값) - umask = 775

666 - 002 = 664
777 - 002 = 775

664 apple.txt
남들도 쓸 수 있도록 접근 권한 변경: chmod 666 apple.txt

cf.
ls = list
ls -l = list long
ls -a = list all (숨겨진 파일도 본다)
ls -l -a = list long all
ls -a -l = list all long
ls -la = ls -al

리눅스에서 . 으로 시작하는 모든 파일은 숨겨진 파일
e.g) .env 환경 변수 파일인데, 아무나 볼 수 없도록 설정

chmod 666 apple.txt
chmod 600 computer.js

file: 666 - 002 (umask) = 664
directory: 777 - 002 = 775


* 글자로도 권한 부여 가능 (내가 원하는 것을 '더하거나 뺄 때' 주로 사용)

apple.txt 파일에 other에 쓰기 권한을
더하기: chmod o+w apple.txt
빼기: chmod o-w apple.txt

chmod g+rw apple.txt // group에 read write 권한 더하기

chmod ug-rw apple.txt // user, group에서 apple.txt 파일에 대한 rw 권한 빼기


(실습)
touch로 파일 생성, rm로 파일 삭제
chmod 숫자로 권한 부여, 글자로 권한 부여


* 사용자 추가
adduser // 아무나 추가할 수는 없다.
sudo adduser
dean 1234

만들어진 계정 확인: cat /etc/passwd
만들어진 비밀번호: cat /etc/shadow

새로 만든 계정으로 접속하려면
ssh -i C:\Users\계정명\.ssh\LightsailDefaultKey-ap-northeast-2.pem ubuntu@서버IPAddress
이건 사용 불가능 (identity 파일이 ubuntu 계정용 키(신분증)이므로. 남의 집 열쇠로 문을 열고 들어갈 수 없음)
→ ssh dean@서버IPAddress
ID/PW 로그인이 가능하기는 한데 보안에서 취약하므로 막아둔 것이다.
(이 방법이 쉽기는 한데, 해커에게도 쉬워서 문제다!)
※ 현재 학습이 목적이라서 해보는 것일뿐, 실무에서는 절대로 ID/PW를 사욯아면 안 된다.

/etc/ssh/ssh_config // Client 설정
/etc/ssh/sshd_config // Server 설정

cd /etc/ssh
sudo nano /etc/ssh/sshd_config
PasswordAuthentication yes (주석 기호인 # 제거. 주석되어 있어도 안 해도 무관)

버전이 올라가면서 예전에 없던 설정 파일이 생겼다.
위 파일은 주석이 있어서 그대로 둬도 무관하고, 아래 파일을 수정해야 비밀번호 접속 '시도' 가능
cd /etc/ssh/sshd_config.d
sudo nano 60-cloudimg-settings.conf
PasswordAuthentication yes


이제 아래와 같은 사용자가 존재
root
ubuntu // root 권한을 빌려쓸 수 있는 사용자 (관리자 권한을 갖고 있는 사용자)
dean // 일반 사용자 (root 권한을 빌려쓸 수 없는 사용자)

ubuntu 계정에서 sudo 권한을 빼면?
시스템에 관리자 권한이 있는 사용자가 아무도 없다.
(서버 기본 설정이 root 계정으로는 접속 불가능하므로)


dean 계정의 home directory
/home/dean
cd /home
ls -al
drwxr-x---  3 jsk    jsk    4096 Jan 21 11:34 jsk
다른 사용자에게 dean 계정의 home directory 접근 권한 부여: chmod o+x dean →
ubuntu 계정이 /home/dean 디렉토리에 들어갈(접근할) 수는 있음
그러나 ls -al 명령을 실행하면 권한이 없다고 나온다. 읽기 권한이 없기 때문

dean 계정이 chmod o+r dean 하면 ubuntu 계정이 ls -al 등 읽기 작업 가능
dean 계정이 chmod o+w dean 하면 ubuntu 계정이 ls -al 등 쓰기 작업까지 가능
(이렇게 하면 계정이 위험해질 수 있어서 ls -al로 봤을 때 디렉토리가 하이라이트된다.)

dean 계정이 chmod o-w dean 하면 다른 계정이 이 디렉토리에 쓰기 작업 불가능
만약 /home/dean 디렉토리에 들어가서 한다면 chmod o-w . (점은 현재 디렉토리를 의미)


id/pw 는 취약한데 실습. 올바른 방법으로 로그인하는 방법은? id/key 기반 인증
키가 있는 계정의 키 기반 인증 방법 알아보기

cf. 윈도우즈에서 내 키파일에 아무나 접근 가능하면?
ssh 서버가 우려하나? No! 서버는 클라이언트의 폴더나 키 위치를 알 수 없고 알 필요도 없다.
ssh 클라이언트가 우려한다.
키 파일은 안전한 곳(나만 볼 수 있고 남은 볼 수 없는 곳)에 보관해야 한다.

mkdir .ssh
chmod 700 .ssh // 나만 읽고 쓰고 접근할 수 있는 디렉토리
(※ 이 디렉토리 권한을 잘못 설정하면,
해커가 아래 authorized_keys 파일 내용에 자기 공개키를 넣는 식으로 을 수정하는 등 해킹을 할 수 있다.)
cd .ssh
ls -al
authorized_keys // 600. 키 파일은 나만 읽고 쓸 수 있으면 된다. 실행 권한은 필요 없음


PKI (Public Key Infrastructure): 비대칭키 (암호화 키 ↔ 복호화 키)
고전 암호: 대칭키 (암호화 키 = 복호화 키)

C:\Users\계정명\.ssh
id_rsa or id_ed25519
등의 개인키가 있을 것이다. ※ 이건 나만 보관하고 있어야 한다.

id_rsa.pub or id_ed25519.pub
등의 공개키 파일도 있을 것이다.
→ 키로 로그인하고 싶은 서버의 인가 키 파일에 내 공개키를 붙여넣는다.
(d

내 계정 홈 디렉토리에서 아래 작업
1. mkdir .ssh
중요한 디렉토리라 기본 설정값이 숨김 상태라서 그냥 ls -l로 하면 안 보이고 ls -al로 해야 보인다.
기본 권한이 775 로 되어 있는데, 이대로 두면 위험해서 접근 불가능
chmod 700 .ssh 로 변경해야 접근 가능하다 (내가 해본 바로는 775 상태에서도 들어가졌음...)

2. chmod 700 .ssh
3. touch authorized_keys
4. chmod 600 authorized_keys
5. nano authorized_keys
공개키 붙여넣기 (ssh-rsa로 출발해서 끝(키 이름)까지 붙여넣는다.)
6. cat authorized_keys
7. 오타는 없는지, 권한과 내용이 맞는지 한 번 더 확인한다.
8. 접속 가능한지 확인
ssh -i 개인키파일명 계정명@서버IP주소
id_rsa가 기본 경로의 기본 키 파일이라서, 이걸 사용할 경우에는 [ssh 계정명@서버IP주소] 만으로 접속 가능

ssh -i C:\Users\계정명\.ssh\LightsailDefaultKey-ap-northeast-2.pem ubuntu@서버IPAddress
ssh -i id_rsa dean@서버IPAddress


Ubuntu 20.04까지는 다른 사용자가 내 홈 디렉토리에 접근할 수 있었다.
24.04부터는 불가능해졌다.
이 때문에 .ssh 디렉토리에 대한 작동 방식이 달라졌다.
(위에 디렉토리 권한이 700, 키 파일 권한이 600이 아니어도 작동하는 이유)

ubuntu 계정 안의 authorized_keys 안에는 내가 공개키를 붙여넣은 적이 없는데 왜 있는지?
윈도우 운영체제가 캐싱
내가 해당 서버에 접속했을 때 사용했던 크리덴션


id/pw 로그인 허용 설정 (시스템을 취약하게 만드는 설정) 취소
1. /etc/ssh/sshd_config.d 디렉토리에서 60-cloudimg-settings.conf 파일 수정: PasswordAuthentication no
2. /etc/ssh 디렉토리에서 sshd_config 파일 수정: #PasswordAuthentication yes (문장 주석 처리)


위애서 비밀번호 로그인을 금지했으므로, 계정을 생성할 때 비밀번호를 저장해봤자 소용 없다.
∴ 애초에 사용자를 생성할 때 password를 사용하지 못 하도록 설정한 다음에 계정 생성:
sudo adduser user9 --disabled-password

chmod // change mode
chown  // change owner (소유권 변경)

관리자가 할 것
sudo adduser user9 --disabled-password
sudo mkdir /home/user9/.ssh
sudo touch /home/user9/.ssh/authorized_keys
sudo nano /home/user9/.ssh/authorized_keys
사용자의 공개키를 이 파일 내용에 붙여넣고 저장 후 종료
sudo chmod 700 /home/user9/.ssh
sudo chmod 600 /home/user9/.ssh/authorized_keys
sudo chown user9:user9 /home/user9/.ssh
sudo chown user9:user9 /home/user9/.ssh/authorized_keys


GPT에게 요청
"ubuntu 서버에 계정을 생성할 때마다 위와 같은 작업을 반복해야 하는데,
이런 작업을 매번 수작업으로 할 수는 없잖아.
위 명령어를 shell script로 만들어줘"

sudo nano create_user.sh 식으로 파일 편집기를 열고 받은 스크립트를 붙여넣고 저장
chmod 700 create_user.sh 로 권한 수정
다음과 같은 식으로 생성한 쉘스크립트 파일 실행: ./create_user.sh 계정명


** sudo를 하지 않고, 특정 파일이나 디렉토리에 특정 그룹에게 rwx 권한을 주는 방법 (nginx 디렉토리 기준)
sudo addgroup sesac
cat /etc/group 에서 확인
sudo chown root:sesac /var/www/html // 해당 디렉토리의 그룹 변경

sudo usermod -a -G sesac ubuntu // sesac 이라는 'G'roup을 ubuntu 계정에 'a'ppend
sudo usermod -aG sesac ubuntu
sudo usermod -aG sesac user9
후략

※ 변경된 권한은 서버에 로그인하는 시점에 반영되므로, 변경된 권한을 사용하려면 서버에 재접속해야 한다.

문제: 위에서 그룹 권한을 줬어도, 그 전에 이미 만들어진 파일의 권한은 그대로이다.
일괄 변경하려면 →
sudo chown -R root:sesac /var/www/html/  // -R(Recursive): 디렉토리 안의 모든 파일
sudo chmod 664 -R /var/www/html/*
sudo chmod 775 /var/www/html/assets

/var/www/html  이 디렉토리는 755로 남아있는데 괜찮나?
※ 파일에 664 권한을 줄 때, 해당 파일이 있는 디렉토리는 775 권한을 주는 게 기본이다.
user/group/other 각각 +1 +1 +1 하는 셈 (즉, 모든 사용자에게 디렉토리 access 권한 추가)
