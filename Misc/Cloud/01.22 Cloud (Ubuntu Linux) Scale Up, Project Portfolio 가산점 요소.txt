
ubuntu@서버IPAddress
ssh -i C:\Users\계정명\.ssh\KeyFilename.pem ubuntu@서버IPAddress

집에서 하느라고 js-1 instance 중지시키고, js-2 instance 새로 생성 @서버IPAddress
ssh ubuntu@서버IPAddress
(EC2 사용하실거라고 함)


sudo lastb // 로그인 실패 기록
sudo lastb | wc -l


logrotate // 로그 로테이션. 리눅스에 있는 서비스 중 하나

웹서비스를 만들 때 적절한 로그가 있는 게 좋다.
3rd party package 중 가장 많이 쓰는 로거: Winston
morgan처럼 사용이 어렵지 않다.
한 블럭(열몇 줄의 코드)만 추가하면 된다.
다만, 로거가 어떻게 돌아가는지 설정은 어떻게 했는지 설명할 수 있어야 면접 가산점


* Project(Portfolio)에 추가하면 가산점이 될 사항:
기술적인 사항
- swagger  // API specification
- morgan  // HTTP access log
- winston  // log level and rotation
- CI/CD  // 빌드 배포 자동화
- HTTPS  // 보안

사업적인 사항
- SEO  // 최적화
e.g. 페이지마다 H1 한 개. 각 페이지는 A 태그로 연결. meta 태그로 각종 키워드 추가, ARIA 태그로 웹 접근성 향상. GA(Google Analytics)
- 시각화와 사용자 분석. 데이터 기반의 insight 도출하기

※ 단, 웹사이트가 잘 만들어졌을 때 얘기다.
완성되지 않아서 오류가 발생하거나, 페이지 수가 너무 적거나 하는 상황이라면 위와 같은 것을 하기 보다는 웹사이트 완성도를 높이는 게 더 낫다.


lightsail은 IT는 잘 모르는 작은 회사나 개인이 편하게 사용하도록 만든 것
제대로 된 IT 회사라면 EC2를 사용한다. 오늘은 EC2로 수업.


EC2 (=ECC; Elastic Cloud Compute. 가상의 자원을 탄력적으로 사용)
VPC (기본 VPC. 보통 회사가 인프라스트럭쳐를 처음 구축할 때 담당자가 한 번만 설정)

VPC 생성은 생략
EC2 생성
(자동으로 되어 있는 게 아무 것도 없어서 내가 직접 설정해야 한다)
- Ubuntu Server 24.04 LTS
- t2.micro (1 CPU, 1GB RAM) 얼마 전에(2026년 1월 기준) 기본이 t3.micro로 바꼈다는 듯 (free tier 사용 가능)
(cf. t2.nano < t2.micro < ".small < medium < large < xlarge, 2xlarge)
  쓸 일 없음  1core/1GB   1/2GB      2/4GB    2/8GB

스펙이 높아질수록 비용이 2배 (cf. t2.micro : 월 1만 원. 작은 사이트는 t2.small ~ medium이면 충분)
범용적으로 t2, t3 시리즈를 많이 사용했는데, 요새는 주로 t3 계열을 사용
t3
t3a // 요새는 t3a가 더 유명)
(cf. PC를 구매할 때 Intel, AMD 사이에서 어느 것을 구매할지 조사하는 것처럼, EC2는 t3, t3a (AMD))

cf. S3 (Storage) 장점: Disk 용량이 다 차면 알아서 늘어나므로, 신경쓸 필요가 없다.

ap-northeast-2 Seoul region

SG(Security Group)
Key Pair // 서버마다 다른 키를 사용하는 게 보안면에서는 제일 좋지만, 관리하기 어려운 단점이 있다.

EC2 → 우측 상단 Region에서 서울 ap-northeast-2 대신 미국(버지니아 북부) us-east-1 선택

인스턴스 시작
1. 인스턴스 이름 지정: 이름 이니셜-1
2. O/S image: Ubuntu 24.04 LTS
3. CPU: t3.micro (기본값)
4. 키 페어 생성
  4-1. 이름: 각자의 이름-sesac-key
  4-2. 유형 RSA / 키 파일 형식 .pem 으로 생성
  4-3. 내려받은 키 파일을 안전한 위치에 저장
5. 네트워크 설정
네트워크 설정 부분은 중급반에서 다룰 내용이라서 우리는 pass~ (기본값으로 사용하면 되니, 가볍게 확인만 한다)
  5-1. Default VPC 유지
  5-2. Public IP 할당. 확인만 한다
  5-3. SG (Security Group. 보안 그룹) 생성
  'launch-wizard-1' 대로 만들지 말 것 !!!
  위에 검은색 배경 메뉴 왼쪽 EC2 버튼을 우클릭해서 새 탭으로 연다.
  왼쪽 메뉴에서 '보안그룹'을 찾고, 거기에서 보안그룹 생성
  5-4. 보안 그룹 생성
  위로 스크롤해서 보안 그룹 이름 칼럼이 보이면 이니셜-sesac-sg 입력
  ※ outbound 규칙은 수정하지 않는다! inbound만 수정할 것이다.
  5-5. 인바운드 규칙 → [규칙 추가]
  유형 SSH, Port 22 (template이라서 자동 선택), 소스 Anywhere IPv4 (0.0.0.0/0), 설명 (선택 사항이지만 작성하는 게 좋은 습관) SSH → [보안 그룹 생성]
6. 원래 탭(5-3에서 새 탭을 열기 전 탭)으로 돌아가서, 위에서 만든 보안그룹 선택 (안 보이면 새로고침 버튼 클릭)
7. 저장소는 그대로 8GB 유지
8. 고급 세부정보를 한 번 훑어본다. 설정은 바꾸지 않는다.
9. 인스턴스 시작
10.  초록색 줄에서 내 Instance ID 클릭
  
※ 강사님이 설명하지 않으셨거나 모르는 설정은 기본값으로 둔다.

public IP / DNS
private IP / DNS

Ubuntu 기본 계정: ubuntu
(cf. Amazon Linux: ec2-user / Redhat: root)

ssh -i KeyFilename.pem ubuntu@서버IPAddress

df disk 용량 확인
df -h 조금 더 자세하게 출력
(-h: help or human-friendly option)

systemctl status nginx
apt install nginx
sudo apt install nginx
systemctl status nginx

curl localhost
웹브라우저에서 public ip로 nginx 웹페이지 접속 가능한지 확인 (불가능)

내 SG (Security Group) 선택 → [Inbound 규칙 편집] → [규칙 추가]
유형 HTTP (TCP 80), 소스 Anywhere IPv4 (0.0.0.0/0), 설명 Web Server 
[규칙 저장]

cd /var/www/html
sudo touch index.html
sudo nano index.html
아무 말이나 입력, 저장 후 웹브라우저에서 페이지 확인

/var/www/html 에서
sudo chown -R root:ubuntu .
sudo chmod -R g+w .
(-R: Recursive (지정 디렉토리(. 이니까 지금 있는 디렉토리)에 있는 모든 파일 대상으로 명령어 실행)

sudo addgroup devs
sudo usermod -aG devs ubuntu
newgrp devs
sudo chown -R root:devs .
sudo chmod -R g+w .


VSCode가 SSH로 서버에 접속해서 VSCode Server를 설치
그 후로는 웹소켓 비슷한 기술을 사용해서 VS Code와 서버가 데이터를 송수신하면서 빠른 속도로 GUI로 내용을 렌더링한다.

newgrp devs or killall node (VSCode-Server 프로세스를 종료)


서버 사양이 부족해졌을 때

방법 1.
서버 등의 CPU 사양, RAM 용량을 변경하는 것: scale up / down
위 작업을 하려면 서버를 잠시 끄고 작업해야 하므로, 서비스 장애 발생

방법 2.
서비스 장애 없이 확장하려면 서버를 한 대 더 추가
그 이후에는 무엇을 해야 하나? (이미지 복제?), 로드 밸런서
Scale-out / Scale-in


Intel Xeon(R)
2 Core 1GB

cat /proc/cpuinfo
processor 0, 1
model name: Intel(R) Xeon(R)
Platinum 8259CL CPU @ 2.50Ghz

cat /proc/meminfo
MemTotal: 


* Scale-up 해보기
인스턴스 중지 (※ 인스턴스 종료(삭제 = Terminate (폐기)) 작업하면 절대 안 됨)
인스턴스를 종료하면 모든 게 다 삭제되고, DoD, NIST 품질 인증까지 받은 것이라서 포렌식 복구도 불가능
(Cloud는 임대(rental) 자원이므로 남의 데이터를 가져갈 수 있으면 안 된다)

[인스턴스 중지] 선택하면 '중지중' → '중지됨'으로 변경됨
그 다음에 인스턴스 선택 → 작업 → 인스턴스 설정 → 인스턴스 유형 변경 → [인스턴스 유형 변경]

t3.micro → t3.medium 으로 변경: 2 core 1GB → 2 core 4GB / 비용은 4배 상승 (2단계 업그레이드이므로)
IP 주소 OOO 로 변경됨

cat /proc/meminfo
MemTotal: 3929044 kB

- 한 번 더 실습
t3.medium → t3a.small 로 변경: 2 core 4GB → 2 core 2GB / 비용은 1/2로 하락

micro → small → medium
     x 2     x 2
      ←       ←
    x 0.5   x 0.5

ubuntu@서버IPAddress
ssh -i KeyFilename.pem ubuntu@서버IPAddress


* HDD 증설
다행이 Hot-swap을 지원해서 전원을 끄지 않고 교체 가능
방법 1. 용량을 늘린다
방법 2. 새로운 디스크를 추가한다.

추가가 더 쉬워서 먼저
AWS Instance → 스토리지
/dev/sda1
/dev/sdxxxx
/dev/sdb (cf. dev = device)
EBS (Elastic Block Store)
EC2는 CPU/RAM을 말한 거였고, EBS틀 통해서 디스크를 붙이는 것

가용 영역(availability-zone) 물리적인 데이터센터
ap-northeast-2
ap-northeast-2a
ap-northeast-2b
ap-northeast-2c
ap-northeast-2d  부산

나의 CPU/RAM은 서울에 있는데 disk는 부산에 있으면? 엄청 느릴 것이다.
그래서 다른 지역에 있는 ebs를 붙일 수 없게 설정되어 있다.
→ 가용역역에서 use1-az5(us-east-1f) 선택. 10GB

생성한 볼륨2를 선택
작업 → 볼륨 연결 → 내 EC2 인스턴스 찾아서 선택 → 디바이스 이름: dev/sdf

sudo fdisk -l

Disk /dev/nvme0n1: 8 GiB, 8589934592 bytes, 16777216 sectors

Disk /dev/nvme1n1: 10 GiB, 10737418240 bytes, 20971520 sectors

F drive
D drive

디스크를 마운트(mount) 해야 한다.

sudo mkdir /data
sudo mount /dev/nvme1n1 /data
"mount: /data: wrong fs type, bad option, bad superblock on /dev/nvme1n1,
missing codepage or helper program, or other error.
dmesg(1) may have more information after failed mount system call" 오류 발생

sudo mkfs -t ext4 /dev/nvme1n1
sudo mkfs mkfs.ext4 /dev/nvme1n1
sudo mount /dev/nvme1n1 /data
sudo chown ubuntu:ubuntu /data

df -h
sudo reboot
리부팅 후에 다시 df -h 하면 마운트된 디스크가 사라졌다. (lsblk로는 볼 수 있다.)
→ 리부팅 후 자동으로 마운트하도록 설정해야 한다
/etc/fstab

sudo blkid /dev/nvme1n1 실행 후 UUID 적어놓기
1eda5fd0-d185-46fe-988c-c6ac67aad752
(엣날 방식은 UUID가 아니라 /dev/nvme1n1 /data 식으로 처리)

sudo nano /etc/fstab
UUID=1eda5fd0-d185-46fe-988c-c6ac67aad752 /data ext4  defaults,nofail 0 2
sudo mount -a (다시 불러오는 것)
"sudo systemctl daemon-reload"를 실행하라는 오류 메시지 → 실행
sudo mount -a (다시 불러오는 것)

/dev/nvme1n1 /data

fdisk -l // 물리적 디스크 확인
lsblk // 블럭 디바이스 확인
df -h // 논리적 디스크 크기 확인

EBS
js-ebs-2 선택 → 작업 → 볼륨 수정
10GB → 15GB로 수정

마운트하기 전에 했던 것은? format
sudo mkfs.ext4 /dev/nvme1n1 // ※ 전부 포맷 (데이터가 다 날아가니, 정말 포맷하려는 게 아니면 하지 말 것!)
sudo resize2fs /dev/nvme1n1 // 늘어난 용량만큼 추가 포맷
df -h로 논리 디스크 크기가 늘어났는지 확인
/data 디렉토리 안에 파일이 잘 있는지 확인 (포맷했다면 기본 파일밖에 없을 것이다)

이번에는 root 디스크 증량
EBS → js-ebs-1 선택 → 작업 → 볼륨 수정: 8GB → 12GB

sudo resize2fs /dev/nvme1n1 명령어로 늘어난 용량만큼 추가 포맷하려고 해도 파티션이 있어서 불가능

sudo growpart /dev/nvme0n1 1 (물리 디스크에서 첫 번째 파티션)
sudo resize2fs /dev/nvme1n1 p1 (partition 1)

sudo df -h


※ A/I 시대에 중요한 것은 '판단 역량'
암기해서 입력하고, 복사하고 붙여넣는 것은 중요하지 않다.


요구사항 명세서 우선순위 설정
우선순위 1. 핵심 기능부터 구현
우선순위 2. 로그인 등. 시간이 부족하면 뺄 것
우선순위 3. 시간이 부족해서 못 하게 될 확률이 매우 높다.
